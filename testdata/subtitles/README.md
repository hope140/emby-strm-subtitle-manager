# 字幕测试数据

此目录预留给 M1/M2/M3 的脱敏、可公开保存的字幕样本。当前不引入虚构的“真实字幕”或私有 Provider 内容。

后续样本至少覆盖正常简中 SRT/ASS、中英和中日双语 ASS、坏 HTML/ASS 标签、错误编码、异常时间轴、普通样式与高特效 ASS，以及 M3 的普通媒体对应证据、STRM 标题/年份匹配、语言冲突、Health `WARNING`、无目标语言正文、候选隔离和候选间时间轴共识场景。M3 只允许使用可公开保存且已脱敏的样本做本地门禁测试；真实媒体和 Provider 内容仍按本机验收流程处理。
