# v0.4.0 C92 默认 closed 部署验收

日期：2026-08-26

范围：只更新 C92 上的 SubBridge app 服务到公开 `v0.4.0` 不可变镜像。未修改 Emby、SH、FRP、OpenResty、媒体内容、媒体目录权限或任何 D2/D3 功能开关。

## 发布物与回滚

- GitHub Release：`v0.4.0`
- 源码 revision：`39da0f0a49310448c9cfa1f8568c3a9a0f18fd0f`
- 运行镜像：`ghcr.io/hope140/subbridge@sha256:48da8b527e6b673afa819460c66a88ff52cae21e622cde76ab19e2e14a6a4594`
- OCI version：`v0.4.0`
- OCI source：`https://github.com/hope140/subbridge`

部署从既有 private closed Compose 派生出版本化副本，只替换 app 镜像为上述 digest。旧 closed Compose 与旧镜像保留，可用于 app-only 回滚；本报告不记录私有路径、凭据或管理员 environment 值。

## 预检与部署

部署前确认旧 app healthy，运行 UID/GID 为 `10001:10001`，host network、只读 rootfs、只读媒体挂载和两个功能开关均保持 closed。新 Compose 合成检查通过，并以 `--no-build` 仅 pull/recreate app。

file-source config 的 `uid/gid/mode` 设置在目标 Docker Compose 上被忽略。非凭据 config 的宿主权限按既有运行约定规范为 `root:root 0644`，随后使用同一 closed Compose app-only recreate。三个 Secret 和管理员 environment 均未打印或修改；仅用应用 Bearer Secret 在进程内发起了一次已认证健康探针。

## 验收结果

| 检查 | 结果 |
|---|---|
| app 容器 | running / healthy，restart count 0 |
| 镜像与 OCI revision | 与 `v0.4.0` Release/digest/revision 一致 |
| 运行隔离 | UID/GID `10001:10001`、rootfs 只读、host network |
| 媒体挂载 | `/media` 为只读 |
| config 与三项 Secret | 实际运行 UID 可读 |
| C92 本机 `/livez`、`/readyz` | 均为 200 |
| 未认证 `/v1/health` | 401 |
| 已认证 `/v1/health` | `status=ok`、`emby_status=ready`、`write_enabled=false`、`remote_search_enabled=false` |
| 既有公网 HTTPS | `/readyz` 为 200，未认证 `/v1/health` 为 401 |

## 未执行范围

本次是默认 closed 发布部署，不执行 Provider Search、Fetch、Preview、Upload、Add、Replace、Delete、Restore、MediaStreams、官方字幕流或实际客户端播放。普通本地媒体与多源 STRM 的独立验收状态不因本次部署改变。

重新启用任何搜索或写入能力仍需独立授权、实时预检、受控样本和 closed 收尾。全新环境安装、升级与回滚演练仍是 M3 的剩余验收项。
