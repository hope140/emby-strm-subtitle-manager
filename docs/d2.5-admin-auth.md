# D2.5 发布版管理员认证

状态：D2.5-A/B 已在本地实现并通过代码与浏览器验证；未部署 C92/SH，未开启 D3 或任何写能力。

本阶段把发布镜像的人机登录和自动化调用分开。普通管理员使用 Docker Secret 配置的用户名和密码登录内嵌只读 UI；CLI、定时任务和 CI 继续使用独立 Bearer Token。当前仍是单实例、单管理员模型，不提供注册、改密码、注销、找回密码、用户列表或多角色后台。

## 配置契约

发布版 Compose 示例新增两个 file-source Secret：

```yaml
security:
  admin_username_file: /run/secrets/app_admin_username
  admin_password_file: /run/secrets/app_admin_password
  session_cookie_secure: false
  session_ttl_seconds: 28800
```

- 用户名长度为 1–64 字节，不接受首尾空白、空白字符或控制字符。
- 密码长度为 12–256 字节，不接受控制字符；密码可以包含空格。
- 管理员密码不能复用 Emby API Key、应用 Bearer Token 或 identity secret（包括 identity 的标准 Base64 形式）。
- Secret 文件允许一个结尾换行，其他换行、控制字符和越界长度都会拒绝。
- 没有通用默认密码。Linux 宿主机应实际将两个文件设为 `10001:10001`、`0400`，并在容器内以应用用户执行 `test -r` 预检；Compose 的 `uid/gid/mode` 不能替代宿主机权限核对。
- 为兼容尚未迁移的内部配置，运行时仍接受仅有 Bearer 的旧配置；此时 UI 登录会明确返回 `admin_login_unavailable`，发布 Compose 模板不采用这种配置。

密码只在启动时从 Secret 读取，进程内保存带随机盐的 PBKDF2-HMAC-SHA256 派生校验材料，不写入配置、镜像、Git、日志、URL 或响应。Secret 轮换方式是替换文件并重建容器；重建也会让所有旧会话失效。

## HTTP 会话

`POST /v1/auth/login` 是唯一不需要先认证的 `/v1` 路由，只接受无 query 的 JSON 用户名和密码。成功后返回不含凭据的状态 JSON，并设置：

- Cookie 名称 `emby_strm_admin_session`
- `HttpOnly`
- `SameSite=Lax`
- `Path=/`
- 默认 8 小时固定 TTL，可配置范围为 60 秒至 24 小时
- `security.session_cookie_secure=true` 时增加 `Secure`，公网 HTTPS 应开启它；内网 HTTP 示例保留 `false`

会话只保存在内存，服务重启后全部失效；服务端只保存会话 Token 的 SHA-256 摘要。登录失败按服务端观察到的远端地址做有界限速：5 分钟内 5 次失败后锁定 1 分钟，状态表和会话表都有固定上限。错误响应不回显用户名、密码、Cookie 或上游内容。

所有其他 `/v1/*` 路由都接受有效管理员会话 Cookie 或 `Authorization: Bearer <token>`。Bearer 仍然只用于自动化，拒绝 query token，当前尚未提供细粒度 scope；不能把它宣称为 D3 权限系统。

## UI 行为

页面只显示管理员用户名和密码输入框。密码在提交后立即清空，JavaScript 不读取或写入 `localStorage`、`sessionStorage`、可读 Cookie、URL、DOM `data-*` 属性、控制台或日志。页面不提供注销或改密按钮；重新加载页面会回到登录界面，服务端会话仍受固定 TTL 控制。会话过期时 API 的 401 会把页面重新切回登录界面。

D2 Search、Fetch、Preview 仍是只读、默认关闭、单源 fail closed 的原有契约；管理员会话不会扩大 D2 能力，也不会打开 `write_enabled`。

## 验证与未完成门禁

本地已验证：

- `internal/auth` 的凭据校验、PBKDF2 派生材料、会话 TTL、重启等价的内存失效、失败限速和固定上限测试
- HTTP 登录错误/成功、HttpOnly/Lax/Secure Cookie、会话访问、Bearer 自动化兼容、query token 拒绝和日志脱敏测试
- Fake Emby Playwright 流程的用户名密码登录、D2 Search/Fetch/Preview、刷新回登录界面、浏览器存储为空和候选敏感字段隔离
- Compose 示例的五个基础 Secret、只读媒体挂载和无 D2 cache/allowlist 依赖检查

仍未完成：

- 未在 C92 或 SH 部署、重启或变更 FRP/OpenResty；真实公网登录验收待单独部署任务
- D2.5-C 的 Bearer 最小权限 scope 尚未实现；当前 Bearer 是单一只读自动化凭据
- CSRF 和任何 D3 写入授权仍未实现，`write_enabled=false` 必须继续保持
- 真实多 MediaSource 样本仍未找到，多源搜索继续安全拒绝

## Knowledge Review

任务或阶段：D2.5-A/B 发布版管理员认证与会话。

验证范围：`internal/auth`、`internal/config`、`internal/httpapi`、内嵌 UI、Fake Emby 浏览器流程、Compose 示例和脱敏日志。

Knowledge Findings：

- 新增约束：管理员人机登录使用 HttpOnly 会话；自动化保留独立 Bearer，二者不共用输入界面或凭据。
- 隐蔽坑：浏览器 JavaScript 看不到 HttpOnly Cookie，因此 E2E 的 `document.cookie` 为空不能证明服务端没有会话；必须同时检查 Set-Cookie 属性和带 Cookie 的 API 请求。
- 被证明错误的假设：旧的“UI 只在内存保存 Bearer”描述已不适用于发布版，UI 现在不接收 Bearer。
- 建议沉淀项：登录失败限速和会话上限必须保持有界；Secret 文件权限要在容器内以应用 UID 实测。

分流判断：

- `docs/lessons-learned.md`：更新管理员会话/HttpOnly 与 E2E 证据边界。
- `docs/architecture.md`：更新认证数据流和当前路由事实。
- `docs/adr/`：ADR-006 保留为方案 C 的长期决策，补充实现状态；scope 与 D3 仍未接受。
- `LOCAL_OPERATIONS.md`：只补充待部署的 Secret 文件角色，不记录凭据内容或本轮任务流水账。
